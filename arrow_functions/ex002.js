/*function createFullName(firstName, lastName) {
    return firstName + " " + lastName;
  }*/

  const createFullName = (firstName, lastName) => firstName + " " + lastName;

  console.log(createFullName("Vinicius", "Melo"));